import {AppProvider} from './appContext';
import {ToolBarProvider} from './ToolBarContext';
import {ThemeProvider} from './themeContext';

export const contextsList = [AppProvider, ToolBarProvider, ThemeProvider];
