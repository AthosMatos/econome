import {ToolBarProvider} from './ToolBarContext';
import {ThemeProvider} from './themeContext';
import {DBProvider} from '../database/DBContext';

export const contextsList = [ToolBarProvider, ThemeProvider, DBProvider];
